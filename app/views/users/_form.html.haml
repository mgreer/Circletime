= simple_form_for @user do |f|
  .inputs
    = f.input :name
    = f.input :stars
    = f.input :location
    = f.input :email
    = f.association :memberships, :through => :circle
  .actions
    = f.button :submit