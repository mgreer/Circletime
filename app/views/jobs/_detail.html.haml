%p
  - if @job.worker.nil?
    #{@job.user} needs a babysitter.
  - else
    - if current_user == @job.user
      #{@job.worker.name} will be your #{@job.job_type.name}.
    - elsif current_user == @job.worker
      You will be #{@job.user}'s #{@job.job_type.name}.
    - else
      #{@job.worker.name} will be #{@job.user}'s #{@job.job_type.name}.


%p
  Date: #{@job.date.strftime("%A, %B %d")} <br/>
  - unless @job.job_type.is_misc
    - if @job.work_unit.hours < 23
      Time: #{@job.time_in_zone.strftime("%I:%M%p (%Z)")} <br/>
    #{pluralize(@job.duration,@job.work_unit.name)} 

- unless @job.description.empty?
  %p
    Details:<br/>
    #{@job.description}

%p
  For: #{@job.user}<br/>
  phone: 231242423
  [call] [text]<br/>
  email: #{@job.user.email}
  [email]<br/>

%p
  Need to cancel this?
  [Why? form and cancel button]
