%html
  %head
  %body
    %p Hiya,
    %p 
    - unless @job.job_type.is_misc
      Thanks for being my #{@job.job_type.name}!
    - else
      Thanks for the #{@job.job_type.name}!
    %p    
      I'll see you on
      %b
        #{@job.date.strftime("%b %d")} 
        - if @job.work_unit.hours < 23
          at #{@job.time_in_zone.strftime("%I:%M%p (%Z)")} 

      see you then!
    %br
    = @job.user.name
      
